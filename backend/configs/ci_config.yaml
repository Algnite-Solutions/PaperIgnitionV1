APP_SERVICE:
  host: http://localhost:8000

USER_DB:
  db_user: ci_user
  db_password: ci_password
  db_host: localhost
  db_port: "5432"
  db_name: ci_user_db

OPENAI_SERVICE:
  base_url: https://api.deepseek.com
  api_key: fake-key-for-ci

dashscope:
  api_key: fake-key-for-ci
  base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  embedding_model: text-embedding-v4
  embedding_dimension: 1536

aliyun_rds:
  enabled: true
  db_user: ci_user
  db_password: ci_password
  db_host: localhost
  db_port: "5432"
  db_name_paper: ci_paper_db

aliyun_oss:
  enabled: false
